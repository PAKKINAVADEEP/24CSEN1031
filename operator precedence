#include <stdio.h>


int main() {
    int a;
    printf("Enter an integer (positive or negative): ");
    scanf("%d", &a);
    if ((a > 0)&& (a %2 ==0)) 
      printf("The nubmber %d is positive and even", a);
    else if   ((a>0)&& (a %2 == 1))
        printf("\nThe number%d is positive and odd ",a);
     return 0;
}
OUTPUT:
Enter an integer (positive or negative): 5

The number5 is positive and odd 
